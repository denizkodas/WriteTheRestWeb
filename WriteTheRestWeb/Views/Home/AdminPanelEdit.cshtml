
<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Hikaye Düzenle</title>
    <link rel="stylesheet" href="~/css/AdminPanelEdit.css" />
</head>
<body>
    <div class="edit-container">
        <h2 class="edit-header">@* @Model?.Name *@ Hikayesini Düzenle</h2>

        <form asp-action="AdminPanelEdit" asp-controller="Home" method="post" class="edit-form">
            <input type="hidden" name="Id" value="@* @Model?.Id *@" />

            <label for="Name">Hikaye Adı</label>
            <input type="text" id="Name" name="Name" value="@* @Model?.Name *@" required />

            <label for="Content">Hikaye İçeriği</label>
            <textarea id="Content" name="Content" rows="6" required>@* @Model?.Content *@</textarea>

            <button type="submit">Kaydet</button>
        </form>
    </div>
</body>
</html>
