
<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@* @Model.Name *@</title>
    <link rel="stylesheet" href="~/css/WatchStory.css" />
</head>
<body>
    <div class="story-container">
        <h1 class="story-title">@* @Model.Name *@</h1>
        <div class="story-content" id="storyText">
            <p>@* @Model.Content *@</p>
        </div>
    </div>

    <script>
        document.addEventListener("DOMContentLoaded", function () {
            const content = document.getElementById("storyText");
            // Gerekirse içerik üzerinde işlem yapılabilir
            if (content.innerText.trim().length === 0) {
                content.innerHTML = "<em>Bu hikayenin içeriği bulunamadı.</em>";
            }
        });
    </script>
</body>
</html>