
<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Profilim</title>
    <link rel="stylesheet" href="~/css/Profile.css" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" />
</head>
<body>
    <div class="profile-container">
        <div class="profile-card">
            <div class="profile-image">
                <img src="~/images/default-user.png" alt="Profil Fotoğrafı" />
            </div>
            <div class="profile-info">
                <h2>@* @Model.Username *@ Kullanıcı Adı</h2>
                <p class="email">
                    <i class="fas fa-envelope"></i> Email Adresi

                  @*   @if (Model.EmailConfirmed)
                    {
                        <i class="fas fa-check-circle verified"></i>
                    } *@
                    @* else
                    { *@
                        <form method="post" asp-controller="Account" asp-action="EmailVerification" style="display:inline; margin-left: 10px;">
                            <button type="submit" class="btn verify-btn"><i class="fas fa-check"></i> Doğrula</button>
                        </form>
                    @* } *@
                </p>
                <p class="bio"><i class="fas fa-user"></i> Hakkımda kısmı burada yer alır.</p>

                <div class="button-container">
                    <form method="post" asp-controller="Home" asp-action="ProfileEdit" style="display:inline;">
                        <button type="submit" class="btn edit"><i class="fas fa-user-edit"></i> Profili Düzenle</button>
                    </form>

                    <form method="post" asp-controller="Account" asp-action="Login" style="display:inline;">
                        <button type="submit" class="btn logout"><i class="fas fa-sign-out-alt"></i> Çıkış Yap</button>
                    </form>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
